---
name: Getting Started
route: /
---

# Getting Started

School Bus is a React component library which provides developers with the easiest possible way of building [Lesson Desk](https://www.lessondesk.com/)’s products.

## Main goals

- Create consistency across products.
- Improve overall quality of the codebase.
- Provide a base set of components that can be reused in many contexts.

## Installation

Make sure `react`, `react-dom`, `formik`, `prop-types` & `styled-components` is installed and run:

```sh
npm install @lessondesk/schoolbus
```
