---
name: Button
---

import { Playground, Props } from 'docz'
import Button from './button'

# Button

The Button component represents a clickable button, which can be used in forms or anywhere in a document that needs simple, standard button functionality.

## Props

<Props of={Button} />

## Variants

<Playground>
  <Button variant={Button.VARIANTS.primary} margin="10px" onClick={() => alert('Clicked!')}>Primary</Button>
  <Button variant={Button.VARIANTS.secondary}  margin="10px" onClick={() => alert('Clicked!')}>Secondary</Button>
  <Button variant={Button.VARIANTS.disabled} margin="10px" onClick={() => alert('Clicked!')}>Disabled</Button>
  <Button variant={Button.VARIANTS.minimal} margin="10px" onClick={() => alert('Clicked!')}>Minimal</Button>
  <Button variant={Button.VARIANTS.delete} margin="10px" onClick={() => alert('Clicked!')}>Delete</Button>
</Playground>

## Sizes

<Playground>
  <Button size={Button.SIZES.compact} margin="10px" onClick={() => alert('Clicked!')}>Compact</Button>
  <Button size={Button.SIZES.default}  margin="10px" onClick={() => alert('Clicked!')}>Default</Button>
  <Button size={Button.SIZES.large}  margin="10px" onClick={() => alert('Clicked!')}>large</Button>
</Playground>

## Shapes

<Playground>
  <Button shape={Button.SHAPES.square} margin="10px" onClick={() => alert('Clicked!')}>Square</Button>
  <Button shape={Button.SHAPES.rounded} margin="10px" onClick={() => alert('Clicked!')}>Round</Button>
  <Button shape={Button.SHAPES.block} margin="10px" onClick={() => alert('Clicked!')}>Block</Button>
</Playground>
